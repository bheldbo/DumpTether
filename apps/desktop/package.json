{
  "name": "@dumptether/desktop",
  "private": true,
  "version": "0.1.3",
  "type": "module",
  "scripts": {
    "configure:client": "node ../../scripts/configure-client.mjs",
    "configure:client:check": "node ../../scripts/configure-client.mjs --check",
    "dev": "npm run configure:client && npm run build:sidecar:dev && tauri dev",
    "build": "npm run build:desktop",
    "build:sidecar": "node scripts/build-sidecar.mjs",
    "build:sidecar:dev": "node scripts/build-sidecar.mjs --self-contained false",
    "build:desktop:exe": "npm run configure:client && npm run build:sidecar && tauri build --no-bundle",
    "build:desktop": "npm run configure:client && npm run build:sidecar && tauri build",
    "build:desktop:msi": "npm run configure:client && npm run build:sidecar && tauri build --bundles msi",
    "build:desktop:linux": "npm run configure:client && npm run build:sidecar && tauri build --bundles appimage,deb,rpm"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
